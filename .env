# Local development environment variables
# This file is ignored by git (.gitignore). Do NOT put real production secrets here.

# Razorpay test keys (replace with live keys in Render dashboard env vars when going live)
RAZORPAY_KEY_ID=rzp_test_qw2SELyvbYRujK
RAZORPAY_KEY_SECRET=1oxZ3xePo9YQxA9Xi2Vbdb8e

# Node environment
NODE_ENV=development

# JWT secret (use a long random string in production)
JWT_SECRET=your_jwt_secret

# Mongo connection (update with your managed MongoDB URI for production)
MONGODB_URI=mongodb://localhost:27017
MONGODB_DBNAME=cokhaenergyfoods

# Optional: override server port locally
PORT=5000

# Frontend (Vite) may use VITE_* vars; define VITE_API_BASE locally if you want to bypass proxy
# VITE_API_BASE=https://choclatestore.onrender.com